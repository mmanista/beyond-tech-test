import { ButtonElement } from '../../elements';

export const VideosListComponent = {
    getPreviousPageButton: () => new ButtonElement('prevPage'),
    getNextPageButton: () => new ButtonElement('nextPage'),
};
